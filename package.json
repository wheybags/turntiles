{
  "name": "orientile",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build src/web/main.ts --outdir site --sourcemap=linked",
    "build-watch": "bun build src/web/main.ts --outdir site --sourcemap=linked --watch"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
